% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tax_state_special_cases.R
\name{calculate_state_cdctc_credit}
\alias{calculate_state_cdctc_credit}
\title{Calculate State Child and Dependent Care Tax Credit (CDCTC)}
\usage{
calculate_state_cdctc_credit(calculations_df, tax_state_credits_df)
}
\arguments{
\item{calculations_df}{Dataframe with \code{starting_income}, \code{household_type},
\code{state_filing_status} (optional), \code{children}, \code{child_care_cost},
and \code{cdctc_credit}}

\item{tax_state_credits_df}{State credit parameters already filtered to
year/state}
}
\value{
Dataframe with \code{state_cdctc_credit} column added
}
\description{
Computes state child and dependent care credits from \code{tax_state_credits_df}
rows for \code{variable_name == "child_dependent_care"}, using one or more
\code{calculation_method} values. This function handles common generic pathways
and delegates legacy NY pre-2026 logic to \code{\link[=apply_NY_pre2026_cdctc]{apply_NY_pre2026_cdctc()}}.
}
\details{
Supported methods in this function:
\itemize{
\item \code{percent_of_fed_cdctc}: Apply a matched percentage to \code{cdctc_credit}
\item \code{percent_of_fed_cdctc_estimate}: Compute a federal-style estimate from
either bracketed or non-bracketed federal-rate parameters, then use that
estimate as the state credit basis
}

Matching behavior:
\itemize{
\item If a method's rows have income brackets, match by filing status + income
(with \code{filing_status == "all"} fallback), plus optional exact
\code{num_children} match (\code{NA} fallback).
\item If a method's rows have \code{income_min}/\code{income_max} all \code{NA}, match directly
by filing status (and optional \code{num_children}) without bracket lookup.
}

Assumptions:
\itemize{
\item State AGI proxy is \code{starting_income}
\item Federal CDCTC amount is already present in \code{cdctc_credit}
\item \code{tax_state_credits_df} is pre-filtered to one year/state
}
}
