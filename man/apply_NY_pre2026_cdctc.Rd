% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tax_state_special_cases.R
\name{apply_NY_pre2026_cdctc}
\alias{apply_NY_pre2026_cdctc}
\title{Apply NY Pre-2026 Child and Dependent Care Tax Credit (Legacy)}
\usage{
apply_NY_pre2026_cdctc(calculations_df, cdcc_rows, bracket_lookup_children)
}
\arguments{
\item{calculations_df}{Dataframe with \code{starting_income},
\code{state_filing_status}, \code{children}, and \code{child_care_cost}}

\item{cdcc_rows}{Subset of state credits parameters for
\code{variable_name == "child_dependent_care"} (already filtered to year/state)}

\item{bracket_lookup_children}{Function used to match bracket values by
income, filing status, and optional \code{num_children} with NA fallback}
}
\value{
Numeric vector of NY pre-2026 CDCTC amounts (coalesced to 0)
}
\description{
Computes New York's pre-2026 child and dependent care credit using a
federal-based structure. The calculation uses three parameter components
from \code{tax_state_credits_df} rows where
\code{calculation_method == "ny_pre2026_federal_brackets"}:
}
\details{
\enumerate{
\item A federal-like rate lookup by income (\code{federal_rate_bracket} /
\code{federal_bracket_rate})
\item A New York coefficient lookup by income (\code{ny_coefficient_bracket} /
\code{ny_coefficient})
\item A child-count-specific cap on allowable childcare expenses
(\code{child_expense_cap} / \code{expense_cap})
}

The function annualizes childcare costs (\code{child_care_cost * 12}), applies
the child-based cap, computes a federal-style estimate, then applies the
NY coefficient:

\code{credit = min(annual_child_care_cost, child_cap) * fed_rate * ny_coefficient}

This is legacy logic retained for historical tax years (through 2025) and is
not intended for future NY policy years.
}
