% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tax_state_special_cases.R
\name{build_state_eitc_lookup}
\alias{build_state_eitc_lookup}
\title{Build State EITC Lookup Table}
\usage{
build_state_eitc_lookup(eitc_lookup_df)
}
\arguments{
\item{eitc_lookup_df}{State EITC lookup table already filtered to year/state,
with \code{income_min}, \code{income_max}, and \code{value_0} through \code{value_3} columns}
}
\value{
Named list with \code{table} (long-format lookup keyed by \code{bracket_idx}
and \code{ca_eitc_children}, also carrying \code{eitc_income_max}) and \code{breaks}
(sorted \code{income_min} vector for \code{\link[=findInterval]{findInterval()}})
}
\description{
Pre-processes the state EITC lookup CSV (wide format with one value column
per child count) into a long-format table keyed by \verb{(bracket_idx, ca_eitc_children)}, plus an \code{income_min} breaks vector for
\code{\link[=findInterval]{findInterval()}}. Called once before the solver loop so that
\code{\link[=apply_CA_eitc]{apply_CA_eitc()}} can do an O(n log m) bracket lookup each iteration
instead of a per-iteration fuzzyjoin.
}
