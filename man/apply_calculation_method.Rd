% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tax_functions.R
\name{apply_calculation_method}
\alias{apply_calculation_method}
\title{Apply a Calculation Method to a Value Vector}
\usage{
apply_calculation_method(
  value_vector,
  method,
  calculations_df,
  var_name = "unknown",
  strict = TRUE
)
}
\arguments{
\item{value_vector}{Numeric vector of raw parameter values (e.g., joined \code{value} column)}

\item{method}{Single string matching a supported calculation_method}

\item{calculations_df}{Dataframe row-aligned with \code{value_vector}, supplying context
columns (household_size, adult, children, children_under6, children_6plus,
final_federal_income_tax, eitc_credit, cdctc_credit)}

\item{var_name}{Variable name used in the error/warning message when the method is unrecognized}

\item{strict}{If TRUE (default), an unrecognized \code{method} triggers \code{stop()}. If FALSE,
it triggers a \code{warning()} and returns 0.}
}
\value{
Numeric vector the same length as \code{value_vector}
}
\description{
Transforms a vector of raw parameter values (typically joined from a tax
parameter CSV) into per-row computed amounts by applying one of the
supported \code{calculation_method} strings. This is the shared dispatch function
used by both state taxable-income adjustment logic and state credit logic --
any new method needed by a state's CSV data should be added here.
}
\details{
Supported methods: \code{fixed} / \code{flat} / \code{flag} (return value as-is),
\code{per_person}, \code{per_adult}, \code{per_child}, \code{per_child_minus1},
\code{per_child_under6_double}, \code{per_child_under6}, \code{per_child_6plus},
\code{percent_of_fed_tax}, \code{percent_of_fed_eitc}, \code{percent_of_fed_cdctc}.
Any unrecognized method is an error when \code{strict = TRUE} (the default), or
a warning that returns 0 when \code{strict = FALSE}.
}
